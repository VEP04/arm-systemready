x_case_runner: &case_runner |
  #!/usr/bin/env python3
  from __future__ import annotations

  import importlib.util
  import sys
  from pathlib import Path

  def main() -> int:
      if len(sys.argv) != 2:
          print("usage: case_runner.py <target_script>", file=sys.stderr)
          return 2

      work_dir = Path.cwd()
      target = Path(sys.argv[1]).resolve()

      spec = importlib.util.spec_from_file_location(
          "runtime_checker_under_test",
          target,
      )
      if spec is None or spec.loader is None:
          print(f"failed to load target: {target}", file=sys.stderr)
          return 2

      module = importlib.util.module_from_spec(spec)
      sys.modules[spec.name] = module
      spec.loader.exec_module(module)

      module.DTS_PATH = work_dir / "device_tree.dts"
      module.MEMMAP_PATH = work_dir / "memmap.log"
      module.OUT_LOG_PATH = work_dir / "runtime_device_mapping_conflict_test.log"
      module._LOG_FH = None

      try:
          module.main()
      except SystemExit as exc:
          code = exc.code
          if isinstance(code, int):
              return code
          return 1

      log_path = work_dir / "runtime_device_mapping_conflict_test.log"
      if log_path.exists():
          print(log_path.read_text(encoding="utf-8", errors="replace"), end="")

      return 0

  if __name__ == "__main__":
      raise SystemExit(main())

suites:
  - name: runtime_device_mapping_conflict_checker
    command: "{dir}/case_runner.py"
    files:
      - common/linux_scripts/runtime_device_mapping_conflict_checker.py

    cases:
      - name: cli_missing_both_inputs_warns
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "DEBUG: DTS file not found"
          - "RESULTS: WARNINGS"
        scripts:
          case_runner.py: *case_runner
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_missing_memmap_warns
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "DEBUG: Memmap file not found"
          - "RESULTS: WARNINGS"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                uart@3000 {
                  reg = <0x0 0x3000 0x0 0x100>;
                };
              };
            };
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_non_overlapping_regions_pass
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "All UEFI Memmap segments CHECKED"
          - "All DTS MMIO ranges CHECKED"
          - "Total segments checked: 2"
          - "Total DTS ranges checked: 1"
          - "No overlaps found between UEFI runtime regions and DTS MMIO ranges"
          - "RESULTS: PASSED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                uart@3000 {
                  reg = <0x0 0x3000 0x0 0x100>;
                };
              };
            };
          memmap.log: |
            RT_Code 0x1000-0x1fff 1 0
            RT_Data 0x5000-0x5fff 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_overlapping_regions_fail
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "RESULTS: FAILED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                uart@3000 {
                  reg = <0x0 0x3000 0x0 0x100>;
                };
              };
            };
          memmap.log: |
            RT_Code 0x3000-0x30ff 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_disabled_node_is_ignored
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "No DTS MMIO ranges extracted."
          - "RESULTS: PASSED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                uart@3000 {
                  status = "disabled";
                  reg = <0x0 0x3000 0x0 0x100>;
                };
              };
            };
          memmap.log: |
            RT_Code 0x3000-0x30ff 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_mmap_reg_name_is_ignored
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "No DTS MMIO ranges extracted."
          - "RESULTS: PASSED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                flash@3000 {
                  reg = <0x0 0x3000 0x0 0x100>;
                  reg-names = "mmap";
                };
              };
            };
          memmap.log: |
            RT_Code 0x3000-0x30ff 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_parent_offset_translation_detects_conflict
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "RESULTS: FAILED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;

                syscon@4000 {
                  reg = <0x0 0x4000 0x0 0x1000>;

                  efuse@100 {
                    reg = <0x0 0x100 0x0 0x20>;
                  };
                };
              };
            };
          memmap.log: |
            RT_Data 0x4100-0x411f 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_reserved_memory_is_ignored
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "No DTS MMIO ranges extracted."
          - "RESULTS: PASSED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              reserved-memory {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;

                carveout@3000 {
                  reg = <0x0 0x3000 0x0 0x100>;
                };
              };
            };
          memmap.log: |
            RT_Code 0x3000-0x30ff 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_utf16_memmap_is_parsed
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "Total segments checked: 1"
          - "RESULTS: PASSED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                uart@3000 {
                  reg = <0x0 0x3000 0x0 0x100>;
                };
              };
            };
        bin_files:
          memmap.log:
            hex: "fffe520054005f0043006f006400650020003000780031003000300030002d0030007800310066006600660020003100200030000a00"
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"

      - name: cli_malformed_memmap_line_is_logged
        type: cli
        args:
          - "{file}"
        expect_exit_code: 0
        expect_output:
          - "Skipped malformed memmap line"
          - "Skipped 1 malformed lines from memmap"
          - "RESULTS: PASSED"
        scripts:
          case_runner.py: *case_runner
          device_tree.dts: |
            /dts-v1/;
            / {
              soc {
                #address-cells = <2>;
                #size-cells = <2>;
                ranges;
                uart@9000 {
                  reg = <0x0 0x9000 0x0 0x100>;
                };
              };
            };
          memmap.log: |
            RT_Code badline
            RT_Data 0x1000-0x1fff 1 0
        post_checks:
          - type: exists
            path: "{dir}/runtime_device_mapping_conflict_test.log"